export const enum AuthRouteNames {
  LOGIN = "LOGIN",
  REGISTER = "REGISTER",
}

export const enum MainRouteNames {
  MENU = "MENU",
}
export const enum UserDetailsRouteNames {
  USER_DETAILS = "USER_DETAILS",
}

export const enum GameRouteNames {
  TABLE = "TABLE",
}
